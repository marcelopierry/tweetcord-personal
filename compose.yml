services:
  Tweetcord:
    image: ghcr.io/yuuzi261/tweetcord:latest
    container_name: tweetcord
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data/:/bot/data
      - ./configs.yml:/bot/configs.yml